coverage:
  status:
    project:
      default:
        # Allow small coverage drops (e.g. when refactoring shuffles a few
        # defensive branches into different files) without failing CI.
        threshold: 0.5%
    patch:
      default:
        target: 85%
