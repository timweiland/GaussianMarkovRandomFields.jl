import{_ as t,o as a,c as i,an as s}from"./chunks/framework.C8cI67RK.js";const g=JSON.parse('{"title":"Automatic Differentiation Reference","description":"","frontmatter":{},"headers":[],"relativePath":"reference/autodiff.md","filePath":"reference/autodiff.md","lastUpdated":null}'),o={name:"reference/autodiff.md"};function n(r,e,d,l,h,c){return a(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="Automatic-Differentiation-Reference" tabindex="-1">Automatic Differentiation Reference <a class="header-anchor" href="#Automatic-Differentiation-Reference" aria-label="Permalink to &quot;Automatic Differentiation Reference {#Automatic-Differentiation-Reference}&quot;">​</a></h1><p>GaussianMarkovRandomFields.jl supports gradient-based inference and optimization through several AD backends. Which backend to reach for depends on <strong>both</strong> the operation and the GMRF type — support is not uniform, and the differences are large enough to matter in practice.</p><h2 id="Two-kinds-of-differentiation" tabindex="-1">Two kinds of differentiation <a class="header-anchor" href="#Two-kinds-of-differentiation" aria-label="Permalink to &quot;Two kinds of differentiation {#Two-kinds-of-differentiation}&quot;">​</a></h2><p>It is worth separating two things that both get called &quot;AD support&quot;, because the answer is different for each.</p><p><strong>Differentiating through GMRF internals</strong> — <code>logpdf</code>, <code>logdetcov</code>, <code>var</code> and <code>gaussian_approximation</code>, with respect to hyperparameters — runs through a sparse Cholesky factorization. No general-purpose AD system can traverse that: CHOLMOD is a C library reached through opaque pointers, and even the pure-Julia multifrontal factorization behind <code>ChordalGMRF</code> is walked incorrectly by backends that try. So every supported combination here rests on a <strong>hand-written rule</strong>, and the support matrix below records exactly which ones exist. This is the part where the choice of backend genuinely constrains what you can do.</p><p><strong>Differentiating your own likelihood</strong> — through <a href="/GaussianMarkovRandomFields.jl/previews/PR181/reference/observation_models#GaussianMarkovRandomFields.AutoDiffObservationModel"><code>AutoDiffObservationModel</code></a> and <a href="/GaussianMarkovRandomFields.jl/previews/PR181/reference/latent_models#GaussianMarkovRandomFields.AutoDiffLatentPrior"><code>AutoDiffLatentPrior</code></a> — is a different situation. There the package differentiates <em>your</em> Julia function, not its own linear algebra, and it does so through <a href="https://github.com/JuliaDiff/DifferentiationInterface.jl" target="_blank" rel="noreferrer">DifferentiationInterface.jl</a>. Any DI-compatible backend can be passed as <code>grad_backend</code>; the package does not need a rule for it. The default picks the first of Enzyme, Mooncake, Zygote or ForwardDiff that is loaded, and you can override it:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DifferentiationInterface, Enzyme</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">obs_model </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> AutoDiffObservationModel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(my_loglik; n_latent </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> n, grad_backend </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> AutoEnzyme</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())</span></span></code></pre></div><p>The support matrix constrains the first kind, not the second.</p><h2 id="Choosing-a-backend" tabindex="-1">Choosing a backend <a class="header-anchor" href="#Choosing-a-backend" aria-label="Permalink to &quot;Choosing a backend {#Choosing-a-backend}&quot;">​</a></h2><p><strong>Start with ForwardDiff.jl.</strong> It is the only backend that currently handles every GMRF type and every differentiable operation in this package, and it is genuinely fast for hyperparameter optimization, where the input is a handful of hyperparameters and forward mode is the right shape for the problem.</p><p>Reach for a reverse-mode backend when the parameter vector is large enough that forward mode&#39;s per-parameter cost dominates:</p><ul><li><p><strong>Zygote.jl</strong> for <code>GMRF</code>, <code>ChordalGMRF</code> and <code>WorkspaceGMRF</code> priors.</p></li><li><p><strong>Mooncake.jl</strong> for anything built on the pure-Julia CliqueTrees factorization — a <code>GMRF</code> constructed with <code>CliqueTreesFactorization()</code>, a <code>WorkspaceGMRF</code> over a <code>CliqueTreesBackend</code>, <code>ChordalGMRF</code>, and constrained versions of all three. It is the only reverse-mode backend that also covers <code>var</code>/<code>std</code>, and its cost per gradient does not grow with the number of hyperparameters. It refuses a CHOLMOD-backed GMRF.</p></li><li><p><strong>Enzyme.jl</strong> for <code>logpdf</code>, <code>logdetcov</code> and <code>gaussian_approximation</code> on <code>GMRF</code> and <code>WorkspaceGMRF</code>. Fast, but it has the most caveats: no <code>var</code>/<code>std</code>, constrained models need Julia 1.12, and <code>ChordalGMRF</code> is unreliable.</p></li></ul><h2 id="Support-matrix" tabindex="-1">Support matrix <a class="header-anchor" href="#Support-matrix" aria-label="Permalink to &quot;Support matrix {#Support-matrix}&quot;">​</a></h2><p>Every cell below was checked against finite differences on Julia 1.10.10, 1.11.9 and 1.12.6, using a precision matrix whose Cholesky factor has genuine fill-in. The fill-in matters: a tridiagonal AR(1) precision has none, and several of these failures are invisible with one.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Operation</th><th style="text-align:right;">ForwardDiff</th><th style="text-align:right;">Zygote</th><th style="text-align:right;">Enzyme</th><th style="text-align:right;">Mooncake</th></tr></thead><tbody><tr><td style="text-align:right;"><code>logdetcov(::GMRF)</code></td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅ CliqueTrees only</td></tr><tr><td style="text-align:right;"><code>logpdf(::GMRF, z)</code></td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅ CliqueTrees only</td></tr><tr><td style="text-align:right;"><code>gaussian_approximation</code> (<code>GMRF</code>)</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅ CliqueTrees only</td></tr><tr><td style="text-align:right;"><code>logdetcov(::ChordalGMRF)</code></td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">⚠️ unreliable</td><td style="text-align:right;">✅</td></tr><tr><td style="text-align:right;"><code>logpdf(::ChordalGMRF, z)</code></td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">⚠️ unreliable</td><td style="text-align:right;">✅</td></tr><tr><td style="text-align:right;"><code>gaussian_approximation</code> (<code>ChordalGMRF</code>)</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">⚠️ unreliable</td><td style="text-align:right;">✅</td></tr><tr><td style="text-align:right;"><code>logdetcov(::WorkspaceGMRF)</code></td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅ CliqueTrees only</td></tr><tr><td style="text-align:right;"><code>logpdf(::WorkspaceGMRF, z)</code></td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅ CliqueTrees only</td></tr><tr><td style="text-align:right;"><code>gaussian_approximation</code> (<code>WorkspaceGMRF</code>)</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅ CliqueTrees only</td></tr><tr><td style="text-align:right;"><code>logpdf(::ConstrainedGMRF, z)</code></td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">Julia 1.12 only</td><td style="text-align:right;">✅ CliqueTrees only</td></tr><tr><td style="text-align:right;"><code>gaussian_approximation</code> (<code>ConstrainedGMRF</code>)</td><td style="text-align:right;">✅</td><td style="text-align:right;">✅</td><td style="text-align:right;">Julia 1.12 only</td><td style="text-align:right;">✅ CliqueTrees only</td></tr><tr><td style="text-align:right;"><code>var</code> / <code>std</code> (<code>GMRF</code>, <code>WorkspaceGMRF</code>)</td><td style="text-align:right;">✅</td><td style="text-align:right;">❌ raises</td><td style="text-align:right;">❌ raises</td><td style="text-align:right;">✅ CliqueTrees only</td></tr><tr><td style="text-align:right;"><code>var</code> / <code>std</code> (<code>ChordalGMRF</code>)</td><td style="text-align:right;">✅</td><td style="text-align:right;">❌ raises</td><td style="text-align:right;">❌ raises</td><td style="text-align:right;">✅</td></tr></tbody></table><p>✅ matches finite differences · ❌ raises an error · ⚠️ see the note for that row</p><p>&quot;CliqueTrees only&quot; means the Mooncake rules need the pure-Julia CliqueTrees factorization rather than CHOLMOD: build the GMRF with <code>CliqueTreesFactorization()</code>, or the workspace with <code>CliqueTreesBackend</code>. On a CHOLMOD-backed one, Mooncake raises. See <a href="/GaussianMarkovRandomFields.jl/previews/PR181/reference/autodiff#Mooncake">Mooncake</a> below.</p><p>The Enzyme <code>ChordalGMRF</code> rows depend on the Julia version <em>and</em> the CPU architecture; see the note under <a href="/GaussianMarkovRandomFields.jl/previews/PR181/reference/autodiff#Enzyme">Enzyme</a> below. The verification above ran on aarch64.</p><p>Every ❌ in this table is an explicit, actionable error. Combinations that cannot be supported raise rather than falling back to differentiating a sparse factorization, because doing the latter produces wrong gradients that no test without a finite-difference reference would catch.</p><h2 id="Zygote" tabindex="-1">Zygote <a class="header-anchor" href="#Zygote" aria-label="Permalink to &quot;Zygote {#Zygote}&quot;">​</a></h2><p>Zygote works through the package&#39;s ChainRules rules, which cover <code>logdetcov</code>, <code>logpdf</code> and <code>gaussian_approximation</code> for every GMRF type in the table. Each one uses a selected inverse rather than differentiating the sparse factorization.</p><p>The workspace reuse path — <code>make_workspace(model; θ...)</code> once, then <code>model(ws; θ...)</code> inside the differentiated function — also works under Zygote. The callable carries an rrule that reruns the model&#39;s <code>mean</code> and <code>precision_matrix</code> construction in reverse mode while keeping the workspace itself (including its <code>update_precision!</code> mutation) out of the trace. <code>SeparableModel</code> is the exception: its Kronecker-product construction is not yet reverse-mode differentiable, so use ForwardDiff for separable models.</p><p><code>var</code> and <code>std</code> raise an <code>ArgumentError</code>. Their tangent needs entries of the covariance <em>outside</em> the precision&#39;s sparsity pattern, which selected inversion does not compute, so there is no cheap rule to write and one built on <code>selinv</code> would be silently wrong. Use ForwardDiff for marginal-variance gradients.</p><p>The contrast with <code>logdetcov</code> is the whole reason that one is supported: <code>∂logdetcov/∂Q = -Q⁻¹</code> is only ever contracted against a <code>dQ</code> living on <code>Q</code>&#39;s own pattern, so the selected inverse is exact there, whereas <code>∂diag(Q⁻¹)/∂Q</code> is not.</p><h2 id="Mooncake" tabindex="-1">Mooncake <a class="header-anchor" href="#Mooncake" aria-label="Permalink to &quot;Mooncake {#Mooncake}&quot;">​</a></h2><p>Mooncake needs a factorization it can traverse, so its support follows the factorization rather than the GMRF type: everything built on <strong>CliqueTrees.jl&#39;s pure-Julia multifrontal Cholesky</strong> is supported, and CHOLMOD is refused.</p><p>Three constructions get you there, and all of them support <code>logdetcov</code>, <code>logpdf</code>, <code>var</code>/<code>std</code> and <code>gaussian_approximation</code>, constrained or not:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GaussianMarkovRandomFields, LinearSolve</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DifferentiationInterface, Mooncake, MooncakeSparse</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> GMRF</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(μ, Q, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">CliqueTreesFactorization</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># a standard GMRF, chordal backend</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> WorkspaceGMRF</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(μ, Q, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">GMRFWorkspace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Q_pattern, CliqueTreesBackend))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ChordalGMRF</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(μ, Q)                        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># the factorization, unwrapped</span></span></code></pre></div><p><code>MooncakeSparse</code> must be loaded alongside <code>Mooncake</code>; the rules for sparse precisions live there.</p><p>A typical hyperparameter objective, with the workspace built once outside the differentiated function so its symbolic factorization is reused:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ws </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> GMRFWorkspace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Q_pattern, CliqueTreesBackend)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> objective</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(θ)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    prior </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> WorkspaceGMRF</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">build_mean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(θ), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">build_precision</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(θ), ws)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    posterior </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> gaussian_approximation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(prior, obs_lik)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> logpdf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(posterior, y)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">grad </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DifferentiationInterface</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gradient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(objective, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AutoMooncake</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), θ)</span></span></code></pre></div><p>Linear equality constraints (sum-to-zero and friends) differentiate on both constrained paths — a <code>ConstrainedGMRF</code> over a CliqueTrees-backed <code>GMRF</code>, and a constrained <code>WorkspaceGMRF</code> — including through <code>mean(posterior)</code>.</p><h3 id="Why-reach-for-it" tabindex="-1">Why reach for it <a class="header-anchor" href="#Why-reach-for-it" aria-label="Permalink to &quot;Why reach for it {#Why-reach-for-it}&quot;">​</a></h3><p>Mooncake is the only reverse-mode backend here that covers <code>var</code>/<code>std</code>, and the only one whose cost is flat in the number of hyperparameters. On a Laplace marginal likelihood over a 64×64 lattice (n = 4096), a gradient costs under 2× a primal evaluation whether the objective has 2 hyperparameters or 32, while ForwardDiff scales linearly in them — about 17× slower than Mooncake at 2 hyperparameters and 53× at 32. Hoist the preparation (<code>DifferentiationInterface.prepare_gradient</code>) out of the optimization loop; re-preparing per call roughly triples the gradient cost.</p><p>With a handful of hyperparameters ForwardDiff is still the simpler choice, and it is the only backend that needs no particular factorization.</p><p><code>gaussian_approximation</code> is differentiated with the Implicit Function Theorem — one differentiable Newton step at the converged mode — rather than through the Fisher-scoring loop, matching the rule the other backends use.</p><h3 id="What-raises" tabindex="-1">What raises <a class="header-anchor" href="#What-raises" aria-label="Permalink to &quot;What raises {#What-raises}&quot;">​</a></h3><p>A CHOLMOD-backed GMRF — what the two-argument constructor resolves to for a general sparse precision — raises an <code>ArgumentError</code> naming the construction to use instead. This is a guard, not an oversight: Mooncake reaches CHOLMOD only as pointers into memory owned by a C library, and left to itself it dereferences one and the process dies with a segmentation fault, taking the rest of the session with it. A <code>WorkspaceGMRF</code> over the default <code>CHOLMODBackend</code> raises the same way.</p><p>The refusal fires on the operations that actually consume the factorization, not on the presence of a GMRF; the parts that need none, such as the mean or the quadratic form behind <code>sqmahal</code>, still differentiate normally.</p><p>Gauss–Newton (<code>NonlinearLeastSquares</code>) likelihoods raise an actionable error under Mooncake, as they do under Zygote and Enzyme: their score needs a forward-mode sparse Jacobian that reverse mode cannot differentiate through. Use ForwardDiff for those.</p><p>As with Enzyme, the <em>error type</em> can vary. Where this package&#39;s guard refuses first you get the <code>ArgumentError</code> naming the operation; where Mooncake&#39;s own rule compiler gives up earlier you get a <code>MooncakeRuleCompilationError</code>, which says nothing about GMRFs. Known cases of the second: <code>rand</code> on every version, and on Julia 1.10 a CHOLMOD-backed GMRF over a precision denser than a tridiagonal one. Both are loud failures — never a wrong number, and, which is the point of the guard, never a crash.</p><div class="tip custom-block"><p class="custom-block-title">CliqueTrees 1.19.5 or newer for <code>var</code> gradients</p><p>Before 1.19.5, CliqueTrees&#39; selected-inversion rule read the selected inverse already projected onto the precision&#39;s sparsity pattern, so entries on the factor&#39;s fill pattern but outside it were silently taken as zero and <code>var</code>/<code>std</code> gradients came out a few percent low. The error was exactly zero for precisions whose pattern is chordal — a tridiagonal AR(1) precision, for instance — which is what made it easy to miss. The package&#39;s compat bound requires the fixed version; <code>logdetcov</code> and <code>logpdf</code> gradients were correct throughout.</p></div><h2 id="Enzyme" tabindex="-1">Enzyme <a class="header-anchor" href="#Enzyme" aria-label="Permalink to &quot;Enzyme {#Enzyme}&quot;">​</a></h2><p>Enzyme differentiates the LLVM IR of whatever it is given, so anything it can reach it will attempt — including CHOLMOD&#39;s <code>ccall</code>s, which carry no derivative information, and the pure-Julia multifrontal factorization behind <code>ChordalGMRF</code>, which it walks and gets wrong. Every operation that consumes a factorization therefore needs an explicit rule, and the package refuses the operations it has no rule for.</p><p>Supported: <code>logdetcov</code>, <code>logpdf</code>, and <code>gaussian_approximation</code>, for <code>GMRF</code>, <code>WorkspaceGMRF</code> and <code>MetaGMRF</code> priors, plus <code>ConstrainedGMRF</code> on Julia 1.12 and <code>ChordalGMRF</code> where Enzyme manages it (both noted below). Gradients flow to the observation likelihood&#39;s hyperparameters as well as the prior&#39;s, and precisions may be sparse, <code>Diagonal</code> or <code>SymTridiagonal</code>.</p><p><code>gaussian_approximation</code> is differentiated with the Implicit Function Theorem rather than through the Fisher-scoring loop, matching the backend-agnostic rule the other backends use.</p><p>Not supported, raising <code>ArgumentError</code>:</p><ul><li><p><code>var</code> and <code>std</code>. Their tangent is <code>-(Q⁻¹ Q̇ Q⁻¹)ᵢᵢ</code>, which reaches entries of <code>Q⁻¹</code> outside the selected-inverse pattern. ForwardDiff handles this by redoing the selected inversion in Dual arithmetic, which reverse mode has no equivalent of, so use ForwardDiff for marginal-variance gradients.</p></li><li><p><code>WorkspaceGMRF</code> with linear equality constraints, whose <code>logpdf</code> carries a constraint-correction term that depends on <code>Q</code>. Use <code>ConstrainedGMRF</code> instead.</p></li></ul><div class="tip custom-block"><p class="custom-block-title">Constrained GMRFs need Julia 1.12 under Enzyme</p><p><code>ConstrainedGMRF</code> stores a bare <code>Float64</code> (<code>log_constraint_correction</code>) alongside its array fields, which makes it a <em>mixed activity</em> type. Enzyme accepts such a type back from a custom rule on Julia 1.12, but on 1.10 and 1.11 it raises <code>MixedReturnException: ... has mixed internal activity types. This is not presently supported.</code> — an upstream limitation, raised before any of this package&#39;s rules run. ForwardDiff and Zygote handle constrained models on every version.</p></div><h3 id="Specify-the-linear-solver-explicitly" tabindex="-1">Specify the linear solver explicitly <a class="header-anchor" href="#Specify-the-linear-solver-explicitly" aria-label="Permalink to &quot;Specify the linear solver explicitly {#Specify-the-linear-solver-explicitly}&quot;">​</a></h3><p>The two-argument <code>GMRF</code> constructor picks a solver by runtime dispatch on the precision matrix type, so its return type infers as <code>Any</code>. Julia 1.12 copes with that, but 1.10 and 1.11 reject the rule outright with <code>AugmentedRuleReturnError: ... a function which returned Any</code>. Always name the algorithm:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">gmrf </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> GMRF</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(μ, Q, LinearSolve</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">CHOLMODFactorization</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># general sparse</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">gmrf </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> GMRF</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(μ, Q, LinearSolve</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">LDLtFactorization</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># SymTridiagonal (AR1, RW1)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">gmrf </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> GMRF</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(μ, Q, LinearSolve</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DiagonalFactorization</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># diagonal (IID)</span></span></code></pre></div><h3 id="Julia-version-differences" tabindex="-1">Julia version differences <a class="header-anchor" href="#Julia-version-differences" aria-label="Permalink to &quot;Julia version differences {#Julia-version-differences}&quot;">​</a></h3><p>Enzyme&#39;s behaviour varies by Julia version even with identical package versions, so the supported set was verified separately on 1.10.10, 1.11.9 and 1.12.6. Unconstrained <code>GMRF</code> and <code>WorkspaceGMRF</code> pipelines behave identically on all three, including diagonal and <code>SymTridiagonal</code> precisions. Two caveats remain, both of them loud failures rather than wrong answers:</p><div class="tip custom-block"><p class="custom-block-title">ChordalGMRF under Enzyme is unreliable</p><p>Whether Enzyme can differentiate a <code>ChordalGMRF</code> depends on the Julia version, the CPU architecture <em>and</em> the resolved package versions: the same Julia 1.12.6 succeeds on aarch64 and fails on x86-64 with <code>EnzymeNoTypeError</code>, and Julia 1.11 fails with <code>EnzymeNoDerivativeError</code>. When it runs it is correct — the package&#39;s rules are the same ones the other GMRF types use, and the test suite checks that a wrong answer is never returned — but it cannot be relied on. Use Mooncake (the backend <code>ChordalGMRF</code> is built for) or ForwardDiff, or run the pipeline on a <code>GMRF</code>/<code>WorkspaceGMRF</code>.</p></div><p>The second is the <code>ConstrainedGMRF</code> mixed-activity limitation described above, which excludes Julia 1.10 and 1.11.</p><p>The <em>error type</em> on unsupported paths can also vary: where this package&#39;s own rule refuses first you get an <code>ArgumentError</code> naming the operation and type, whereas if Enzyme&#39;s own compilation gives up earlier you get an Enzyme error instead. Both are errors, never wrong numbers.</p><h2 id="ForwardDiff" tabindex="-1">ForwardDiff <a class="header-anchor" href="#ForwardDiff" aria-label="Permalink to &quot;ForwardDiff {#ForwardDiff}&quot;">​</a></h2><p>ForwardDiff has Dual-aware paths for every GMRF type and every operation in the table above.</p><p>One caveat on <code>var</code>/<code>std</code>: the GMRF must be built with a solver that supports selected inversion (<code>CHOLMODFactorization</code>, <code>CholeskyFactorization</code>, …). Without one, <code>var</code> falls back to the stochastic <code>RBMCStrategy</code> estimator, whose solves go through the primal factorization and would drop every partial, so it raises instead.</p><p>Second-order derivatives via nested Duals are not supported. For Hessians, use finite differences over a ForwardDiff gradient (<code>FiniteDiff.finite_difference_jacobian</code> of <code>ForwardDiff.gradient</code>), which is more stable than a pure finite-difference Hessian.</p><h2 id="Constrained-GMRFs" tabindex="-1">Constrained GMRFs <a class="header-anchor" href="#Constrained-GMRFs" aria-label="Permalink to &quot;Constrained GMRFs {#Constrained-GMRFs}&quot;">​</a></h2><p><code>ConstrainedGMRF</code> priors (from <code>RW1Model</code>, <code>BesagModel</code>, …) are supported by Zygote, ForwardDiff, Mooncake (over a CliqueTrees-backed base GMRF) and — on Julia 1.12 only — Enzyme. See the note in the Enzyme section for why the older versions are excluded. Constrained <code>WorkspaceGMRF</code>s are supported by the same set except Enzyme, which has no rule for their <code>Q</code>-dependent constraint correction.</p><h2 id="Testing-your-own-gradients" tabindex="-1">Testing your own gradients <a class="header-anchor" href="#Testing-your-own-gradients" aria-label="Permalink to &quot;Testing your own gradients {#Testing-your-own-gradients}&quot;">​</a></h2><p>If you add a model or an observation likelihood, check its gradients against finite differences using a precision matrix <strong>with Cholesky fill-in</strong> — a 2D grid Laplacian, not a tridiagonal AR(1) precision. Several of the bugs behind the table above survived for a long time precisely because the tests used AR(1) precisions, whose factor has no fill-in and so cannot exercise the code path where a selected inverse is written back onto <code>Q</code>&#39;s sparsity pattern.</p><h2 id="See-Also" tabindex="-1">See Also <a class="header-anchor" href="#See-Also" aria-label="Permalink to &quot;See Also {#See-Also}&quot;">​</a></h2><ul><li><p><a href="/GaussianMarkovRandomFields.jl/previews/PR181/tutorials/automatic_differentiation#Automatic-Differentiation-for-GMRF-Hyperparameters">Automatic Differentiation for GMRF Hyperparameters</a>: practical examples</p></li><li><p><a href="https://juliadiff.org/ChainRulesCore.jl/stable/" target="_blank" rel="noreferrer">ChainRulesCore.jl docs</a></p></li><li><p><a href="https://enzyme.mit.edu/julia/stable/" target="_blank" rel="noreferrer">Enzyme.jl docs</a></p></li></ul>`,67)])])}const k=t(o,[["render",n]]);export{g as __pageData,k as default};
